# 谛听 DiTing - 后端依赖
# 核心 Web 框架
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
pydantic>=2.5.0

# 音频处理
numpy>=1.24.0
scipy>=1.11.0
librosa>=0.10.0
soundfile>=0.12.0

# X-ASR 推理引擎 (sherpa-onnx)
# pip install sherpa-onnx
sherpa-onnx>=1.12.0

# 热词同音/模糊拼音归一化
pypinyin>=0.55.0

# 可选会议管理云存储（未配置时自动使用本地 SQLite）
supabase>=2.0.0
